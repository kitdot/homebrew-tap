# kitdot/homebrew-tap

Homebrew tap for [nsh](https://github.com/kitdot/nsh) — SSH host manager for macOS.

## Install

```bash
brew tap kitdot/tap
brew install kitdot/tap/nsh
```

## Uninstall

```bash
brew uninstall nsh
brew untap kitdot/tap
```
